function App(): React.JSX.Element {
  // const ipcHandle = (): void => window.electron.ipcRenderer.send('ping')

  return (
    <>
      <div className="flex flex-col gap-4">
        <div className="w-10 bg-red-100">Test</div>
      </div>
    </>
  )
}

export default App
